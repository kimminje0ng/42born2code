#include "libft.h"

char *strnstr(const char *big, const char *little, size_t len);
{
    size_t i;
    size_t little_size;

    i = 0;
    little_size = ft_strlen(little);
    if (little_size = 0)
        return ((char *)big);
    while () {
        
    }
    
}